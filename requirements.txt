# Root-level copy of server/requirements.txt so Vercel's Python builder
# (which looks for requirements.txt at the project root) can install the
# same dependencies for api/index.py. Keep these two files in sync.
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
langfuse==2.53.9
litellm==1.51.3
motor==3.6.0
google-cloud-aiplatform==1.71.1
